---
export const prerender = false;

import Layout from "../layouts/Layout.astro";
import { getCollection } from "astro:content";
import { getSiteConfig } from "../utils/config";
import Icon from "../components/Icon.astro";

const config = getSiteConfig();

// Get sample data for preview
const alumni = await getCollection("alumni");
const blogPosts = await getCollection("blog");
const jobs = await getCollection("jobs");

// Get featured alumni (first 3)
const featuredAlumni = alumni.slice(0, 3);

// Fetch live upcoming events from MongoDB
let latestEvents: any[] = [];
try {
  const eventsRes = await fetch(`${Astro.url.origin}/api/events?status=upcoming&limit=3`);
  if (eventsRes.ok) {
    const eventsData = await eventsRes.json();
    latestEvents = eventsData.events || [];
  }
} catch (e) {
  latestEvents = [];
}

// Get latest blog posts (first 3)
const latestBlogPosts = blogPosts
  .filter((post) => !post.data.draft)
  .sort(
    (a, b) =>
      new Date(b.data.publishDate).getTime() -
      new Date(a.data.publishDate).getTime()
  )
  .slice(0, 3);

// Get featured jobs (first 3)
const featuredJobs = jobs
  .filter((job) => job.data.featured)
  .sort((a, b) => {
    const dateA = new Date(a.data.posted_date).getTime();
    const dateB = new Date(b.data.posted_date).getTime();
    return dateB - dateA;
  })
  .slice(0, 3);
---

<Layout title={config.site.name} description={config.site.description}>
  <!-- About Section -->
  <section class="section bg-gradient-to-br from-accent-50 to-white">
    <div class="container-custom">
      <div class="grid grid-cols-1 lg:grid-cols-1">
        <div>
          <h2 class="text-3xl text-center md:text-4xl font-bold mb-6 text-gray-900">PrepSOM Alumni Portal</h2>
          <p class="text-lg text-gray-600 text-justify mb-6">
            PrepSOM is an AI-powered interview preparation and campus placement management platform.
            The PrepSOM Alumni Portal is dedicated to fostering lifelong connections between graduates, 
            creating opportunities for collaboration, mentorship, and professional growth within our 
            diverse community of alumni.
          </p>
          <p class="text-lg text-gray-600 text-justify mb-6">
            We believe that the strength of our network lies in the collective wisdom, experience, 
            and achievements of our members. By providing a platform for meaningful engagement, 
            we empower our alumni to support each other's success and contribute to the advancement 
            of their respective fields.
          </p>
          <p class="text-lg text-gray-600 text-justify mb-6">
            Whether you're looking to reconnect with old friends, expand your professional network, 
            or give back to the community through mentorship and collaboration, the PrepSOM Alumni 
            Portal is here to support you on your journey.
          </p>
          <div class="flex flex-col sm:flex-row gap-2 mb-6">
            <div class="flex items-center text-primary-600">
              <svg class="w-6 h-6 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
              </svg>
              <span class="font-medium">Professional Networking</span>
            </div>
            <div class="flex items-center text-primary-600">
              <svg class="w-6 h-6 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
              </svg>
              <span class="font-medium">Mentorship Programs</span>
            </div>
            <div class="flex items-center text-primary-600">
              <svg class="w-6 h-6 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
              </svg>
              <span class="font-medium">Job Opportunities</span>
            </div>
            <div class="flex items-center text-primary-600">
              <svg class="w-6 h-6 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
              </svg>
              <span class="font-medium">Events and Activities</span>
            </div>
          </div>
        </div>
      </div>
    </div>
    <div class="container-custom mt-12">
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
      <div class="card text-center p-8">
          <div class="w-16 h-16 mx-auto mb-6 bg-primary-100 rounded-full flex items-center justify-center">
            <svg class="w-8 h-8 text-primary-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"></path>
            </svg>
          </div>
          <h3 class="text-xl font-semibold mb-4">Building Connections</h3>
          <p class="text-gray-600">
            Connecting alumni across generations, industries, and geographical boundaries
          </p>
        </div>
        <div class="card text-center p-8">
          <div class="w-16 h-16 mx-auto mb-6 bg-primary-100 rounded-full flex items-center justify-center">
            <svg class="w-8 h-8 text-primary-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"></path>
            </svg>
          </div>
          <h3 class="text-xl font-semibold mb-4">Professional Networking</h3>
          <p class="text-gray-600">
            Expand your professional network by connecting with alumni from various industries and backgrounds, fostering valuable relationships and opportunities.
          </p>
        </div>
        <div class="card text-center p-8">
          <div class="w-16 h-16 mx-auto mb-6 bg-primary-100 rounded-full flex items-center justify-center">
            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="w-8 h-8 text-primary-600 bi bi-chat-left-text" viewBox="0 0 16 16">
              <path d="M14 1a1 1 0 0 1 1 1v8a1 1 0 0 1-1 1H4.414A2 2 0 0 0 3 11.586l-2 2V2a1 1 0 0 1 1-1zM2 0a2 2 0 0 0-2 2v12.793a.5.5 0 0 0 .854.353l2.853-2.853A1 1 0 0 1 4.414 12H14a2 2 0 0 0 2-2V2a2 2 0 0 0-2-2z"/>
              <path d="M3 3.5a.5.5 0 0 1 .5-.5h9a.5.5 0 0 1 0 1h-9a.5.5 0 0 1-.5-.5M3 6a.5.5 0 0 1 .5-.5h9a.5.5 0 0 1 0 1h-9A.5.5 0 0 1 3 6m0 2.5a.5.5 0 0 1 .5-.5h5a.5.5 0 0 1 0 1h-5a.5.5 0 0 1-.5-.5"/>
            </svg>
          </div>
          <h3 class="text-xl font-semibold mb-4">Mentorship Programs</h3>
          <p class="text-gray-600">
            Connect with experienced alumni mentors who can provide guidance, support, and insights to help you navigate your career path and achieve your goals.
          </p>
      </div>
      <div class="card text-center p-8">
          <div class="w-16 h-16 mx-auto mb-6 bg-primary-100 rounded-full flex items-center justify-center">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <rect x="2" y="7" width="20" height="16" rx="2" ry="2"></rect>
                <path d="M16 3H8v4h8V3z"></path>
          </div>
          <h3 class="text-xl font-semibold mb-4">Job Opportunities</h3>
          <p class="text-gray-600">
            Access exclusive job and internship opportunities shared by fellow alumni and industry partners, helping you advance your career and explore new paths.
          </p>
        </div>
        <div class="card text-center p-8">
          <div class="w-16 h-16 mx-auto mb-6 bg-primary-100 rounded-full flex items-center justify-center">
             <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <rect x="3" y="4" width="18" height="18" rx="2" ry="2"></rect>
                <line x1="16" y1="2" x2="16" y2="6"></line>
                <line x1="8" y1="2" x2="8" y2="6"></line>
                <line x1="3" y1="10" x2="21" y2="10"></line>
              </svg>
          </div>
          <h3 class="text-xl font-semibold mb-4">Events and Activities</h3>
          <p class="text-gray-600">
            Stay informed about upcoming alumni events, reunions, webinars, and workshops, providing opportunities to connect, learn, and engage with the community.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- Personal Career Coordinator Section -->
  <section class="section bg-gray-50">
    <div class="container-custom">
      <div class="text-center mb-12">
        <h2 class="text-3xl md:text-4xl font-bold mb-4">Your Personal Career Coordinator</h2>
        <p class="text-xl text-justify text-gray-600 max-w-2xl mx-auto">
          Let AI handle the heavy lifting while you focus on what matters most - preparing for your dream career.
          Our AI-powered career coordinator is designed to support you at every step of your career journey. 
          From personalized job recommendations to interview preparation and networking opportunities, 
          we're here to help you succeed in your professional endeavors.  
        </p>
      </div>
      
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        <div class="card text-center p-8">
          <div class="w-16 h-16 mx-auto mb-6 bg-primary-100 rounded-full flex items-center justify-center">
            <svg class="w-8 h-8 text-primary-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"></path>
              <circle cx="12" cy="7" r="4" stroke-linecap="round" stroke-linejoin="round" stroke-width="2"></circle>
            </svg>
          </div>
          <h3 class="text-xl font-semibold mb-4">Manage Profile</h3>
          <p class="text-gray-600">
            Showcase your skills through comprehensive assessments and personalized profile management, ensuring you stand out to potential employers.
          </p>
        </div>

        <div class="card text-center p-8">
          <div class="w-16 h-16 mx-auto mb-6 bg-secondary-100 rounded-full flex items-center justify-center">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <rect x="2" y="7" width="20" height="16" rx="2" ry="2"></rect>
                <path d="M16 3H8v4h8V3z"></path>
              </svg>
          </div>
          <h3 class="text-xl font-semibold mb-4">Auto Apply for Jobs</h3>
          <p class="text-gray-600">
            AI-powered applications to relevant opportunities based on your profile, ensuring you never miss out on a potential job match.
          </p>
        </div>

        <div class="card text-center p-8">
          <div class="w-16 h-16 mx-auto mb-6 bg-primary-100 rounded-full flex items-center justify-center">
            <svg class="w-8 h-8 text-primary-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"></path>
            </svg>
          </div>
          <h3 class="text-xl font-semibold mb-4">Interview Preparation</h3>
          <p class="text-gray-600">
           Industry-leading mock interview tools and feedback to help you prepare for real interviews and boost your confidence.
          </p>
        </div>
      </div>
    </div>
  </section>

    <!-- Get Started Section -->
  <section class="hero section bg-gray-50 text-black">
    <div class="container-custom text-center">
      <h2 class="text-3xl md:text-4xl font-bold mb-6">Get Started</h2>
      <p class="text-xl mb-8 max-w-3xl mx-auto">
        Whether you're an alumni member, a developer, or simply passionate about building 
        stronger communities, we invite you to join us in shaping the future of alumni engagement. We're always looking for talented individuals to contribute to our platform, share their insights, and help us create a vibrant and supportive community. Join us today and be part of something special!
      </p>
      <div class="card mb-8">
        <div class="card bg-white bg-opacity-10 border-black rounded-lg p-6">
          <h3 class="text-xl font-semibold mb-3">Join as Alumni Connect with your Alumni Network</h3>
          <p class="text-black">
           Stay connected with fellow graduates, discover opportunities, and build lasting professional relationships. Join our alumni community and take advantage of the resources, events, and networking opportunities available to you.
          </p>
          <a href="/alumni/register" class="btn btn-primary btn-lg mt-4">
            <Icon name="mdi:account-plus" class="w-5 h-5 mr-2" />
            Sign Up Now
          </a>
        </div>
      </div>
      
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a href="/contact" class="btn btn-outline btn-lg text-white border-white hover:bg-white hover:text-primary-700">
          <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 4.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
          </svg>
          Contact Us
        </a>
      </div>
    </div>
  </section>

  <!-- Stats Section -->
   <!--
  <section class="section-sm bg-gradient-to-r from-accent-50 to-accent-100">
    <div class="container-custom">
      <div class="grid grid-cols-2 md:grid-cols-4 gap-8">
        <div class="text-center">
          <div
            class="w-16 h-16 bg-primary-500 rounded-2xl flex items-center justify-center mx-auto mb-4 shadow-lg"
          >
            <Icon name="mdi:account-group" class="w-8 h-8 text-white" />
          </div>
          <h3 class="text-3xl font-bold text-primary-900 mb-2">
            {alumni.length}+
          </h3>
          <p class="text-accent-700 font-medium">Alumni</p>
        </div>

        <div class="text-center">
          <div
            class="w-16 h-16 bg-secondary-500 rounded-2xl flex items-center justify-center mx-auto mb-4 shadow-lg"
          >
            <Icon name="mdi:school" class="w-8 h-8 text-white" />
          </div>
          <h3 class="text-3xl font-bold text-secondary-900 mb-2">
            {config.faculties.length}
          </h3>
          <p class="text-accent-700 font-medium">Faculties</p>
        </div>

        <div class="text-center">
          <div
            class="w-16 h-16 bg-accent-500 rounded-2xl flex items-center justify-center mx-auto mb-4 shadow-lg"
          >
            <Icon name="mdi:calendar" class="w-8 h-8 text-white" />
          </div>
          <h3 class="text-3xl font-bold text-accent-900 mb-2">
            {config.years.end - config.years.start + 1}
          </h3>
          <p class="text-accent-700 font-medium">Years</p>
        </div>

        <div class="text-center">
          <div
            class="w-16 h-16 bg-primary-600 rounded-2xl flex items-center justify-center mx-auto mb-4 shadow-lg"
          >
            <Icon name="mdi:map-marker" class="w-8 h-8 text-white" />
          </div>
          <h3 class="text-3xl font-bold text-primary-900 mb-2">Global</h3>
          <p class="text-accent-700 font-medium">Network</p>
        </div>
      </div>
    </div>
  </section>
-->

<!-- Job Opportunities Section -->
  <section class="section-md bg-gradient-to-br from-accent-50 to-white">
    <div class="container-custom">
      <!-- Section Header -->
      <div class="text-center mb-12">
        <div class="inline-block mb-4">
          <!-- <span class="badge badge-primary flex items-center space-x-2">
            <Icon name="mdi:briefcase" class="w-4 h-4" />
            <span>Career Opportunities</span>
          </span> -->
        </div>
        <h2 class="text-3xl md:text-5xl font-bold text-accent-900 mt-2 mb-4">
          Job and Internship Board
        </h2>
        <p class="text-lg text-accent-600 max-w-2xl mx-auto text-justify">
          Discover exclusive job and internship opportunities in the actuarial field. Connect with leading employers and advance your career. Our job board features a curated selection of positions from top companies, tailored to our alumni network. Whether you're a recent graduate or an experienced professional, explore the latest openings and take the next step in your career journey.
        </p>
      </div>

      <!-- Featured Jobs Grid
      {featuredJobs.length > 0 ? (
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 mb-8">
          {featuredJobs.map((job) => (
            <div class="bg-white rounded-xl shadow-md hover:shadow-xl transition-shadow duration-300 overflow-hidden border border-accent-100 hover:border-primary-300">
              Job Card Header
              <div class="p-6">
                Job Title & Company
                <div class="mb-4">
                  <h3 class="text-xl font-bold text-accent-900 mb-1">
                    {job.data.title}
                  </h3>
                  <p class="text-accent-600 font-medium">{job.data.company}</p>
                </div>

                Job Meta
                <div class="space-y-3 mb-4 pb-4 border-b border-accent-100">
                  Location
                  <div class="flex items-center space-x-2 text-accent-600">
                    <Icon name="mdi:map-marker" class="w-4 h-4 text-primary-500 flex-shrink-0" />
                    <span class="text-sm">{job.data.location}</span>
                  </div>

                  Salary
                  {job.data.salary && (
                    <div class="flex items-center space-x-2 text-accent-600">
                      <Icon name="mdi:cash" class="w-4 h-4 text-primary-500 flex-shrink-0" />
                      <span class="text-sm font-medium text-green-600">
                        ${job.data.salary.min.toLocaleString()} - ${job.data.salary.max.toLocaleString()}
                      </span>
                    </div>
                  )}

                  Job Type & Level
                  <div class="flex items-center space-x-2">
                    <span class={`badge badge-sm ${
                      job.data.jobType === 'full-time' ? 'bg-green-100 text-green-800' :
                      job.data.jobType === 'part-time' ? 'bg-blue-100 text-blue-800' :
                      job.data.jobType === 'contract' ? 'bg-purple-100 text-purple-800' :
                      'bg-orange-100 text-orange-800'
                    }`}>
                      {job.data.jobType}
                    </span>
                    <span class={`badge badge-sm ${
                      job.data.experienceLevel === 'entry' ? 'bg-emerald-100 text-emerald-800' :
                      job.data.experienceLevel === 'mid' ? 'bg-yellow-100 text-yellow-800' :
                      job.data.experienceLevel === 'senior' ? 'bg-red-100 text-red-800' :
                      'bg-indigo-100 text-indigo-800'
                    }`}>
                      {job.data.experienceLevel}
                    </span>
                  </div>
                </div>

                 Skills
                {job.data.skills && job.data.skills.length > 0 && (
                  <div class="mb-4">
                    <p class="text-xs font-semibold text-accent-500 uppercase tracking-wider mb-2">
                      Required Skills
                    </p>
                    <div class="flex flex-wrap gap-2">
                      {job.data.skills.slice(0, 3).map((skill) => (
                        <span class="text-xs bg-primary-50 text-primary-700 px-2 py-1 rounded-full">
                          {skill}
                        </span>
                      ))}
                      {job.data.skills.length > 3 && (
                        <span class="text-xs bg-accent-100 text-accent-600 px-2 py-1 rounded-full">
                          +{job.data.skills.length - 3}
                        </span>
                      )}
                    </div>
                  </div>
                )}

                Apply Button
                <a
                  href={job.data.url}
                  target="_blank"
                  rel="noopener noreferrer"
                  class="inline-flex items-center justify-center w-full btn btn-primary btn-sm"
                >
                  <Icon name="mdi:open-in-new" class="w-4 h-4 mr-2" />
                  View Job
                </a>
              </div>
            </div>
          ))}
        </div>
      ) : (
        <div class="text-center py-12">
          <Icon name="mdi:briefcase" class="w-16 h-16 mx-auto text-accent-300 mb-4" />
          <p class="text-accent-600">No featured jobs available at the moment.</p>
        </div>
      )}
      -->

      <!-- View All Jobs Button -->
      <div class="text-center">
        <a
          href="/jobs"
          class="inline-flex items-center btn btn-primary btn-lg gap-2"
        >
          <Icon name="mdi:briefcase" class="w-5 h-5" />
          View Job Opportunities
        </a>
        <p class="text-accent-600 text-sm mt-4">
          Browse actuarial positions across the industry
        </p><br/>
      </div>
    </div>
  </section>
      
  <!-- Featured Alumni Section -->
  <section class="section bg-gradient-to-br from-accent-50 to-primary-50">
    <div class="container-custom">
      <div class="text-center mb-12">
        <h2 class="text-4xl md:text-5xl font-bold text-accent-900 mb-4">
          Featured Alumni
        </h2>
        <p class="text-xl text-accent-600 max-w-3xl mx-auto">
          Meet some of our outstanding alumni who are making a difference in
          their fields.
        </p>
      </div>

      <div class="grid-responsive mb-12">
        {
          featuredAlumni.map((alum) => (
            <div class="profile-card text-center group">
              <div class="relative mb-6">
                {alum.data.photo ? (
                  <img
                    src={alum.data.photo}
                    alt={alum.data.name}
                    class="profile-avatar group-hover:scale-105 transition-transform duration-300"
                    loading="lazy"
                  />
                ) : (
                  <div class="profile-avatar-placeholder group-hover:scale-105 transition-transform duration-300">
                    {alum.data.name
                      .split(" ")
                      .map((n) => n[0])
                      .join("")
                      .toUpperCase()}
                  </div>
                )}
                <div class="absolute top-0 right-0">
                  <span class="badge badge-primary">{alum.data.faculty}</span>
                </div>
              </div>

              <h3 class="text-xl font-semibold text-accent-900 mb-2">
                {alum.data.name}
              </h3>
              <p class="text-accent-600 mb-4 line-clamp-3">
                {alum.data.short_bio}
              </p>

              <div class="flex items-center justify-center space-x-2 mb-4">
                {alum.data.linkedin && (
                  <a
                    href={alum.data.linkedin}
                    target="_blank"
                    rel="noopener noreferrer"
                    class="text-accent-400 hover:text-primary-600 transition-colors duration-200"
                  >
                    <Icon name="mdi:linkedin" class="w-5 h-5" />
                  </a>
                )}
                {alum.data.twitter && (
                  <a
                    href={alum.data.twitter}
                    target="_blank"
                    rel="noopener noreferrer"
                    class="text-accent-400 hover:text-primary-600 transition-colors duration-200"
                  >
                    <Icon name="mdi:twitter" class="w-5 h-5" />
                  </a>
                )}
                {alum.data.github && (
                  <a
                    href={alum.data.github}
                    target="_blank"
                    rel="noopener noreferrer"
                    class="text-accent-400 hover:text-primary-600 transition-colors duration-200"
                  >
                    <Icon name="mdi:github" class="w-5 h-5" />
                  </a>
                )}
              </div>

              <div class="text-sm text-accent-500 mb-4">
                Class of {alum.data.year}
              </div>

              <a
                href={`/alumni/profiles/${alum.data.slug}`}
                class="btn btn-primary btn-sm w-full"
              >
                View Profile
              </a>
            </div>
          ))
        }
      </div>

      <div class="text-center">
        <a href="/alumni/profiles" class="btn btn-outline btn-lg">
          <Icon name="mdi:account-multiple" class="w-6 h-6 mr-2" />
          View More Alumni
        </a>
      </div>
    </div>
  </section>

  <!-- Latest Blog Posts Section -->
  <section class="section">
    <div class="container-custom">
      <div class="text-center mb-12">
        <h2 class="text-4xl md:text-5xl font-bold text-accent-900 mb-4">
          Latest News & Insights
        </h2>
        <p class="text-xl text-accent-600 max-w-3xl mx-auto">
          Stay informed with the latest updates, stories, and insights from our
          alumni community.
        </p>
      </div>

      <div class="grid-responsive mb-12">
        {
          latestBlogPosts.map((post) => (
            <div class="blog-card group">
              <div class="relative mb-4">
                {post.data.image ? (
                  <img
                    src={post.data.image}
                    alt={post.data.title}
                    class="w-full h-48 object-cover rounded-xl group-hover:scale-105 transition-transform duration-300"
                    loading="lazy"
                  />
                ) : (
                  <div class="w-full h-48 bg-gradient-to-br from-accent-100 to-primary-100 rounded-xl flex items-center justify-center">
                    <Icon
                      name="mdi:newspaper"
                      class="w-16 h-16 text-accent-400"
                    />
                  </div>
                )}
                <div class="absolute top-4 left-4">
                  <span class="badge badge-secondary">
                    {post.data.category}
                  </span>
                </div>
              </div>

              <h3 class="text-xl font-semibold text-accent-900 mb-2 group-hover:text-primary-600 transition-colors duration-200 line-clamp-2">
                {post.data.title}
              </h3>
              <p class="text-accent-600 mb-4 line-clamp-3">
                {post.data.description}
              </p>

              <div class="flex items-center justify-between text-sm text-accent-500 mb-4">
                <span>
                  {new Date(post.data.publishDate).toLocaleDateString("en-US", {
                    month: "long",
                    day: "numeric",
                    year: "numeric",
                  })}
                </span>
                <span>By {post.data.author}</span>
              </div>

              <a
                href={`/blog/${post.slug}`}
                class="text-primary-600 hover:text-primary-700 font-medium"
              >
                Read More →
              </a>
            </div>
          ))
        }
      </div>

      <div class="text-center">
        <a href="/blog" class="btn btn-outline btn-lg">
          <Icon name="mdi:newspaper" class="w-6 h-6 mr-2" />
          View More Posts
        </a>
      </div>
    </div>
  </section>

    <!-- Latest Events Section -->
  <section class="section bg-gradient-to-br from-accent-50 to-secondary-50">
    <div class="container-custom">
      <div class="text-center mb-12">
        <h2 class="text-4xl md:text-5xl font-bold text-accent-900 mb-4">
          Events and Webinars
        </h2>
        <p class="text-xl text-accent-600 max-w-3xl mx-auto text-justify">
          Stay updated with the latest events, webinars, workshops, discussions, hackathons, reunions, and networking
          opportunities in our alumni community. Explore upcoming events, register for those that interest you, and connect with fellow alumni. Whether you're looking to learn new skills, catch up with old friends, or just have fun, there's something for everyone in our vibrant event calendar. Don't miss out on the chance to engage with your alumni network and make lasting connections!
      </div>

      <div class="grid-responsive mb-12">
        {
          latestEvents.length === 0 ? (
            <div class="text-center py-12 col-span-3">
              <Icon name="mdi:calendar-blank" class="w-16 h-16 mx-auto text-accent-300 mb-4" />
              <p class="text-accent-600">No upcoming events at the moment. Check back soon!</p>
            </div>
          ) : (
            latestEvents.map((event) => {
              const eventType = event.eventType || '';
              return (
                <div class="event-card group">
                  <div class="relative mb-4">
                    <div class={`w-full h-48 rounded-xl flex items-center justify-center ${
                      eventType === 'ama' ? 'bg-gradient-to-br from-purple-100 to-purple-200' :
                      eventType === 'workshop' ? 'bg-gradient-to-br from-green-100 to-green-200' :
                      'bg-gradient-to-br from-primary-100 to-secondary-100'
                    }`}>
                      <Icon name="mdi:calendar-event" class="w-16 h-16 text-primary-400" />
                    </div>
                    <div class="absolute top-4 right-4">
                      <span class="badge badge-primary">
                        {new Date(event.startTime).toLocaleDateString("en-US", { month: "short", day: "numeric" })}
                      </span>
                    </div>
                    <div class="absolute top-4 left-4">
                      <span class="badge badge-secondary uppercase text-xs">{event.eventType}</span>
                    </div>
                  </div>

                  <h3 class="text-xl font-semibold text-accent-900 mb-2 group-hover:text-primary-600 transition-colors duration-200">
                    {event.title}
                  </h3>
                  <p class="text-accent-600 mb-4 line-clamp-2">{event.description}</p>

                  <div class="flex items-center justify-between">
                    <span class="text-sm text-accent-500">
                      {event.confirmedCount ?? 0} / {event.capacity} registered
                    </span>
                    <a href={`/events/${event.slug}`} class="text-primary-600 hover:text-primary-700 font-medium">
                      Learn More →
                    </a>
                  </div>
                </div>
              );
            })
          )
        }
      </div>

      <div class="text-center">
        <a href="/events" class="btn btn-outline btn-lg">
          <Icon name="mdi:calendar-multiple" class="w-6 h-6 mr-2" />
          View All Events
        </a>
      </div>
    </div>
  </section>

  <!-- Call to Action Section -->
  <section class="section bg-gradient-to-br from-primary-600 to-secondary-600">
    <div class="container-custom text-center">
      <h2 class="text-4xl md:text-5xl font-bold text-white mb-6">
        Ready to Connect?
      </h2>
      <p class="text-xl text-white/90 mb-8 max-w-3xl mx-auto">
        Join our thriving alumni network and stay connected with your fellow
        graduates. Discover opportunities, share experiences, and build lasting
        relationships.
      </p>
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a
          href="/alumni"
          class="btn btn-primary btn-lg text-white hover:bg-primary-700 transition-colors duration-200"
        >
          <Icon name="mdi:account-plus" class="w-6 h-6 mr-2" />
          Join the Network
        </a>

        <a
          href="/about"
          class="btn btn-primary btn-lg text-white hover:bg-primary-700 transition-colors duration-200"
        >
          <Icon name="mdi:information" class="w-6 h-6 mr-2" />
          Learn More
        </a>
      </div>
    </div>
  </section>
</Layout>