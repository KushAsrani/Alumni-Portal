---
interface Props {
  name?: string;
  class?: string;
  [key: string]: unknown;
}

const { name = '', class: className = '', ...rest } = Astro.props as Props;
---

<iconify-icon icon={name} class={className} {...rest}></iconify-icon>
